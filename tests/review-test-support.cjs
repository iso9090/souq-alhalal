const fs=require('fs'),path=require('path'),os=require('os'),cp=require('child_process');exports.snapshot=()=>{const root=process.cwd(),out=fs.mkdtempSync(path.join(os.tmpdir(),'melkak-review-build-'));const files=cp.execFileSync('git',['ls-files','--cached','--others','--exclude-standard'],{encoding:'utf8'}).trim().split(/\r?\n/);for(const f of files){if(!fs.existsSync(f)||!fs.statSync(f).isFile())continue;const dest=path.join(out,f);fs.mkdirSync(path.dirname(dest),{recursive:true});fs.copyFileSync(path.join(root,f),dest)}return out};
