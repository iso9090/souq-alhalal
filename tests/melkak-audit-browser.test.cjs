const assert=require('node:assert/strict');const {chromium}=require(process.env.PLAYWRIGHT_MODULE||'C:/Users/asus/AppData/Local/OpenAI/Codex/runtimes/cua_node/b58ca2eaa616c2da/bin/node_modules/playwright-core');(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});try{const p=await b.newPage(),errors=[];p.on('pageerror',e=>errors.push(e.message));await p.goto('http://127.0.0.1:8786/#/admin/listings');await p.locator('#preview-role').selectOption('super_admin');await p.locator('[data-action=moderate][data-id=AE-cars]').click();const reason='  مراجعة <img src=x onerror=alert(1)>  ';await p.locator('#moderate-form textarea').fill(reason);await p.locator('#moderate-form [type=submit]').click();assert.equal(await p.evaluate(()=>window.__melkak.store.state.audit[0].reason),reason);await p.evaluate(()=>location.hash='#/admin/audit');await p.waitForTimeout(150);assert.ok((await p.locator('#main').innerText()).includes(reason.trim()));assert.equal(await p.locator('#main img[src=x]').count(),0);assert.deepEqual(errors,[]);console.log('SUMMARY | 3/3 PASS')}finally{await b.close()}})().catch(e=>{console.error(e);process.exitCode=1});
