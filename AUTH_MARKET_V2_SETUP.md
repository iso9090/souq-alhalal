> تحديث: مسار صور إعلانات الحلال أصبح مجانيًا ومنفصلًا؛ راجع [FREE_LIVESTOCK_IMAGES.md](FREE_LIVESTOCK_IMAGES.md). متطلبات التوقيع المذكورة أدناه لا تنطبق على صور الحلال الجديدة.

# سوق الحلال — إعدادات مرحلة التطوير V2

تحديث المرحلة الثانية: تمت قراءة إعدادات Firebase الفعلية دون كتابة، وأضيف منع تكرار الصور ومعالجة حالات Auth وفحص المالك الصارم. راجع [LIVE_SERVICES_SETUP.md](LIVE_SERVICES_SETUP.md) للحالة الأحدث وخطوات التفعيل والحدود؛ عبارات «لم تُفحص Console» أدناه تصف المرحلة الأولى فقط.

هذه المرحلة محلية فقط. لا نشر أو دمج إلى main، ولا تعديل لقواعد أو حسابات الإنتاج أو Android أو Google Play. الصور المرجعية مصدر للشكل، وليست مصدر بيانات الإعلانات.

## مزود الصور

لم يكن Cloudinary مستخدمًا في النسخة الأصلية؛ كانت الصور JPEG مضغوطة داخل Firestore. النسخة الجديدة تقرأ الصور القديمة كما هي، وتجهز الرفع الموقّع إلى Cloudinary. الإعداد الافتراضي `imageProvider.signingEndpoint` فارغ عمدًا، ولذلك رفع الإعلان الجديد أو صورة Hero غير متاح فعليًا حتى إعداد الخدمة. لا يوجد fallback يكتب Base64 جديدة إلى Firestore.

يلزم إنشاء/تحديد حساب Cloudinary وخدمة HTTPS موثوقة لتوقيع الرفع، بعد الموافقات اللازمة. لا يرسل المالك Secret في المحادثة ولا يضعه في ملفات الموقع. يحفظ API Secret في بيئة الخادم فقط. اسم السحابة وAPI key ليسا أسرار التوقيع؛ تُرجعهما الخدمة للمتصفح مع توقيع محدود.

عقد الخدمة المطلوب:

1. تستقبل `POST` مع Firebase ID token في `Authorization: Bearer …`، وJSON يضم `purpose` بقيمة `listing` أو `hero`، و`bytes` و`contentType`.
2. تتحقق من التوكن والمشروع الصحيح والحساب النشط. لرفع Hero تتحقق من صلاحية المالك الفعلية والسجل المحمي `adminSecurity/config`، لا من قيمة يرسلها العميل.
3. تطبق CORS لأصل الموقع المعتمد فقط، وحدود معدل وحصة لكل UID. تقبل الصور JPEG/PNG/WebP فقط، وتقيد حجم الملف من جهة Cloudinary أيضًا؛ قياس العميل وحده ليس حماية.
4. تنشئ `public_id` عشوائيًا ضمن نطاق المستخدم، و`overwrite=false`، ووقت توقيع حديث. تحدد upload preset موقّعًا يقيّد أنواع الصور والحجم؛ لا تسمح للعميل باختيار public_id أو folder أو preset تعسفي.
5. ترجع `{cloudName, apiKey, signature, params:{timestamp,public_id,upload_preset?,folder?,overwrite:false}}`. التوقيع يغطي القيم نفسها، بما فيها `overwrite` إذا أرسلته الخدمة. لا ترجع API Secret.
6. يحتفظ Firestore بالرابط `https://res.cloudinary.com/<cloud>/image/upload/...` فقط. يمكن اختيار خدمة وسيطة تستقبل الملف وتفحص حجمه ومحتواه على الخادم لضمان أشد. ترتيب التخلص من uploads غير المرتبطة بإعلان يحتاج سياسة مستقلة؛ هذه المرحلة لا تحذف ملفات Cloudinary أو بيانات قديمة.

المحوّل الحالي مكتمل في المتصفح واختُبر بخدمة توقيع وهمية معزولة؛ الخدمة الخلفية الحقيقية وحساب Cloudinary غير منشأين في هذه المرحلة. يلزم تحديد عنوان الخدمة في `image-provider.js` بعد تجهيزها ثم اختبار رفع حقيقي في بيئة تجريبية. المرجع: [Cloudinary authenticated uploads](https://cloudinary.com/documentation/upload_images#authenticated_requests).

## الصور والتوافق

- الإعلانات الجديدة: 1 إلى 3 صور. قواعد التطوير تفرض العدد عند الإنشاء وإضافة/استبدال الصور. لا تتأثر الصور القديمة بأكثر من ثلاث صور عند عرضها أو تعديل بيانات الإعلان دون تغيير الصور. يمكن إزالة صورة قديمة مع الحفاظ على الباقي حتى إن بقيت أكثر من ثلاث.
- المتصفح يفك الصورة، يطبق EXIF orientation، يحافظ على النسبة ويحد البعد الأكبر بـ1600 بكسل. JPEG لا يتجاوز 300 KiB بعد الضغط؛ الصور البسيطة قد تكون أقل من 150 KiB ولا تُضخّم بلا داعٍ. تُرفض الملفات التالفة وغير المدعومة، والملفات الأكبر من 30 MiB أو 80 مليون بكسل.
- معاينة/حذف/استبدال/تقديم/تأخير/اختيار الرئيسية في إضافة حلال، والعداد حتى 3/3. الضغط قد يُعاد قبل الإرسال كتحقق نهائي.
- السلايدر مشترك للبطاقات والتفاصيل، ويعرض كل الصور القديمة الصالحة. يدعم الأسهم والنقاط واللمس ولوحة المفاتيح وlazy loading والصورة البديلة عند الفشل.
- لا تُمنح شارة «بائع موثق» من حقل يمكن للبائع تزويره. لا توجد في المصدر آلية موثوقة لتوثيق البائع يمكن عرضها بهذه الصفة؛ توثيق الحيوان الموجود يبقى في منطق الخدمات، ولا يعاد تسميته توثيق بائع.

## تسجيل الدخول

البريد وكلمة المرور وشروط القوة والتأكيد وأزرار العين محفوظة. الواجهة العامة تعرض Google وFacebook وX والبريد فقط. كود الهاتف القديم محفوظ دون مدخل عام، ولم يفصل أي phone credential.

جميع مزودي OAuth يستخدمون Firebase Auth 12.18.0 الموجود بالمشروع. يبدأ المسار بـPopup على الكمبيوتر ومتصفح الهاتف لتجنب قيود التخزين عبر النطاقات على GitHub Pages، ويستخدم Redirect عند منع Popup. استعادة redirect تعالج الأخطاء والنتيجة الفارغة برسالة. لم يُختبر OAuth حيًا أو تُفحص حالة التفعيل في Console.

الحساب يُحفظ في `users/{uid}` نفسه باستخدام المعاملة الموجودة، دون استبدال الصلاحيات أو الاسم أو نوع الحساب عند الدخول الاجتماعي. تعارض البريد مع مزود آخر يطلب الدخول بالطريقة الأصلية ولا يربط الحسابات تلقائيًا.

الإعداد اليدوي، بعد تصريح منفصل:

- **Email/Password:** تأكد من تفعيل المزود في Firebase Authentication؛ اختبر تسجيل الدخول وإنشاء مستخدم تجريبي واستعادة كلمة المرور بعيدًا عن حساب المالك.
- **Google:** فعّل Google وحدد بريد الدعم والنطاقات المسموح بها. اختبر Popup فعليًا على الكمبيوتر والمتصفح المحمول. [دليل Firebase Google](https://firebase.google.com/docs/auth/web/google-signin).
- **Facebook:** أنشئ/جهز تطبيق Meta مناسبًا، فعّل Facebook Login، واضبط callback الذي يعرضه Firebase (`https://souq-al-halal-9e3e8.firebaseapp.com/__/auth/handler` للإعداد الحالي)، مع سياسة الخصوصية وحذف البيانات ومتطلبات وضع التطبيق. خزّن App ID وApp Secret في إعداد مزود Firebase فقط. [دليل Firebase Facebook](https://firebase.google.com/docs/auth/web/facebook-login).
- **X:** جهّز تطبيق مطور X وإعداد callback المطابق لـFirebase. أدخل API key وAPI secret في مزود Twitter داخل Firebase فقط. الواجهة تسميه X؛ الكود يستخدم `TwitterAuthProvider`. [دليل Firebase Twitter/X](https://firebase.google.com/docs/auth/web/twitter-login).
- **Redirect:** أصل GitHub Pages وauthDomain الحالي مختلفان. التفعيل وحده لا يثبت أن redirect سيعمل على Safari/Chrome مع قيود التخزين. طبق أحد حلول Firebase الرسمية، أو احتفظ بـPopup؛ لا تغيّر authDomain إلى نطاق لا يخدم مساعد Firebase. هذه المرحلة لا تضيف proxy أو استضافة إنتاج. [إرشادات Firebase Redirect](https://firebase.google.com/docs/auth/web/redirect-best-practices).
- **Android WebView:** لم يتغير Android. الكود يرشد مستخدم WebView إلى متصفح الهاتف أو البريد وكلمة المرور؛ لا يدعي تجاوز منع OAuth في المتصفح المضمن. تجربة Samsung الحقيقية مطلوبة لاحقًا قبل AAB.

## حساب المالك

`admin-tools/owner-email-dry-run.mjs` مولد خطة محلي نقي: لا SDK ولا اتصال ولا قراءة حساب إنتاج ولا كتابة ولا إنشاء مستخدم. يرفض UID مختلفًا وبريدًا مستخدمًا ومدخلات لا تمثل مالكًا مسجلاً بالهاتف. لا توجد عملية ترحيل فعلية في الموقع.

بعد موافقة صريحة منفصلة فقط: يسجل المالك الدخول بحسابه الحالي، ويتحقق من UID والصلاحيات والسجل المحمي، ثم يربط `EmailAuthProvider.credential` بواسطة `linkWithCredential` على **currentUser** نفسه. يُعاد التحقق من UID والسجل والبيانات، ثم يجرب دخول البريد في متصفح منفصل ولوحة Admin V2. لا يُنشأ Owner جديد ولا تُنقل السجلات ولا يُفصل الهاتف. البريد وكلمة المرور يُدخلان في واجهة موثوقة دون سجلات أو Git. [ربط الحسابات في Firebase](https://firebase.google.com/docs/auth/web/account-linking).

## Hero والإعلانات المميزة

إضافة مستقلة إلى Admin V2 باسم «إدارة واجهة الصفحة الرئيسية»، للمالك فقط. `homePage/config` وثيقة عامة القراءة، لا يكتبها المستخدمون أو المساعدون. القواعد المحلية تتحقق من الشكل والهوية والرابط وعدد العناصر والتواريخ والأولوية ومنع التكرار. لا ينفّذ الإعلان تفعيلًا لنفسه، ولا يتدخل هذا في خدمات التمييز المدفوعة القديمة.

الحقول: `mode: static|featured`، و`imageUrl`، و`featured` حتى ثلاثة عناصر `{animalId,active,priority,startAt,endAt}`، و`updatedBy/updatedAt`. يعرض كل سوق إعلانات بلده النشطة ذات الصور والمواعيد الصالحة فقط. غياب الإعداد/رفض قراءته/غياب إعلان صالح يعرض الصورة الأصلية. القواعد لم تُنشر؛ حفظ Hero على الإنتاج سيبقى مرفوضًا حتى نشر القواعد بعد اعتمادها.

## التحقق وإعادة التشغيل

`npm test` للوحدات. `npm run test:rules` يتطلب محاكي Firestore على `127.0.0.1` ومتغير `FIRESTORE_EMULATOR_HOST`؛ يرفض الاختبار أي عنوان غير محلي. `npm run test:browser` و`npm run test:v2-browser` يستخدمان Edge مع Playwright وجميع طلبات Firebase معترضة. تحدد `PLAYWRIGHT_MODULE` إذا كانت المكتبة خارج المشروع.

`npm run lint` هو مدقق المشروع المحلي لصحة JavaScript ومعرفات HTML ومراجع الملفات؛ ليس ESLint. `npm run build` ينتج نسخة ثابتة في `dist/` من قائمة ملفات عامة محددة، دون أدوات إدارة أو اختبارات أو نسخ احتياطية. ليس هناك bundler مطلوب لهذا المشروع الثابت، ولا تنفّذ هذه الأوامر نشرًا.

`npm run preview:mock` يشغل معاينة معزولة على `http://127.0.0.1:4174/`؛ البيانات والحسابات اختبارية، وكل عمليات Firebase محاكاة، ولا اتصال بالإنتاج. صور المعاينة مشتقة من صورة المشروع الحالية. المعاينة ليست استضافة عامة.

الخطوات المؤجلة قبل إصدار نهائي: إعداد مزود الصور الحقيقي؛ مراجعة وتفعيل مزودي Auth؛ اعتماد ثم تنفيذ ربط حساب المالك بالـUID نفسه؛ تجربة Samsung كبائع ومشترٍ ومزايد ومالك؛ قرار AAB جديد ثم Google Play Internal Testing. لا Production مباشرة.
