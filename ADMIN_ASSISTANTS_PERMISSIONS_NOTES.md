# المساعدون والصلاحيات — مراجعة محلية فقط

## الوضع الحالي والتوافق

- قبل إنشاء سجل الحماية يستمر الحساب النشط الذي يحمل Firebase Authentication custom claim `admin: true` كـ`super_admin`. بعد إنشاء `adminSecurity/config` يلزم أيضًا وجود معرفه في `superAdminUids`، حتى عندما يكون `enabled: false`. أي claim جديد خارج السجل لا يمنح Super Admin. لا تمنح قيمة `role` وحدها هذه السلطة.
- لا تتغير حسابات Authentication أو ملفات المستخدمين الحالية عند تعيين مساعد أو إزالة دوره. لا توجد كلمات مرور أو بيانات اعتماد جديدة.
- صلاحيات المساعد محفوظة في `adminAccess/{uid}`، بعيدًا عن حقول الملف الشخصي القابلة للتعديل. الملفات العادية لا تقبل إضافة `role` أو `permissions` أو `adminStatus`.
- التفويض معطّل افتراضيًا: غياب إعداد الحماية أو تعطيله يمنع المساعدين ومنح الصلاحيات. المدير القديم يبقى قادرًا على استعمال الأقسام السابقة.
- الوصول إلى لوحة الإدارة اختياري من زر الإدارة؛ لا تفتح تلقائيًا بعد تسجيل الدخول.

## المخطط

`adminAccess/{uid}` للمساعد:

```text
role: admin_assistant
adminStatus: active | suspended
permissions: قائمة أسماء الصلاحيات الـ21 المحددة في admin-permissions.js
createdByAdminUid: معرف من عيّن المساعد
adminCreatedAt: server timestamp
adminUpdatedAt: server timestamp
moderationLogId: معرف سجل التدقيق الذري
```

إزالة الدور تحفظ نفس المستند مع `role: removed` و`adminStatus: suspended` وقائمة صلاحيات فارغة. لا تحذف المستخدم أو حساب Authentication. يمكن إعادة تعيين المستخدم لاحقًا بإجراء إضافة موثق.

`adminSecurity/config` إعداد موثوق لا يسمح لأي عميل بتعديله:

```text
enabled: true | false
superAdminUids: قائمة كاملة بكل معرفات أصحاب admin custom claim الحقيقي
```

يمكن لبيئة التهيئة الموثوقة أيضًا إنشاء `adminAccess/{ownerUid}` مع `role: super_admin` لكل مالك. هذا السجل يحمي الهدف ويتيح عرض اسمه للمساعد المخول بعرض المساعدين؛ لا يمنح دخول Super Admin دون custom claim.

## حماية الصلاحيات

- جميع العمليات تعيد التحقق من الوصول، وتفرض Firestore Rules الصلاحية المحددة في الطلب نفسه. إظهار/إخفاء الواجهة ليس حاجز الأمان الوحيد.
- يمنع تعديل صلاحيات الذات، استهداف المالك، وإنشاء أو ترقية Super Admin من العميل، حتى بواسطة مالك حقيقي.
- المساعد المفوِّض يحتاج صلاحية العملية صراحة، ويجب أن تشمل صلاحياته جميع صلاحيات الهدف القديمة والجديدة. لذلك لا يستطيع خفض أو تعليق أو إزالة دور مساعد أعلى منه عبر إدارة المساعدين.
- إيقاف المساعد يمنع سلطته الإدارية، ويترك حسابه العادي كما هو. تعليق/حظر حساب المستخدم يمنع أيضًا سلطته الإدارية.
- لا تمنح صلاحية الإدارة صلاحية العرض تلقائيًا. اختر عرض القسم مع عمليات الإدارة المطلوبة حتى تكون العمليات متاحة من واجهته.
- الإعلانات والمزادات العامة تظل قابلة للقراءة وفق قواعد الموقع العامة؛ صلاحيات `listings_view` و`auctions_view` تقيد واجهة الإدارة، ولا تجعل البيانات العامة سرية.
- البحث عن مستخدم لإضافته يحتاج صلاحية `assistants_create` ويتيح قراءة دليل المستخدمين. عرض المساعدين وحده يتيح ملفات أصحاب سجل adminAccess، وليس قائمة جميع المستخدمين.
- تغيير الصلاحيات والإيقاف وإعادة التفعيل والإزالة والإضافة معاملات ذرية مرتبطة بسجل Admin Log غير قابل للتعديل أو الحذف. يحتفظ السجل بالفاعل والهدف والسبب والوقت والصلاحيات القديمة والجديدة.
- الأقسام والروابط اليدوية والأزرار ترفض الصلاحيات الناقصة. بيانات سبق تحميلها ليست قابلة للاسترجاع من جهاز المستخدم بعد عرضها؛ Rules تمنع القراءات والكتابات الجديدة بعد سحب الصلاحية.

## البحث وتسجيل الدخول

النظام الحالي يستخدم حسابات Firebase Authentication الموجودة وتسجيل الدخول الحالي. البحث مطابق تمامًا للهاتف المحفوظ أو البريد إن كان محفوظًا في ملف المستخدم. التسجيل الحالي لا يحفظ البريد في Firestore؛ لذلك استخدم الهاتف للملفات التي لا تحتوي بريدًا. لا تنسخ كلمات مرور أو tokens، ولا تنشئ حسابًا من شاشة المساعدين.

ملف Firestore موجود هو دليل تسجيل سابق، وليس فحصًا مباشرًا لحالة حساب Authentication الآن. إذا كان الحساب قد حُذف من Authentication وبقي ملفه، فلن يستطيع تسجيل الدخول أو استعمال الصلاحيات. قبل التعيين الحقيقي تحقّق إداريًا من أن الحساب المقصود ما زال موجودًا؛ فحص حسابات الآخرين مباشرة يحتاج بيئة Backend موثوقة. لم تُنشأ بيانات اعتماد لهذا الغرض.

آخر نشاط إداري للمساعد غير متاح كحقل مجمع حاليًا، وتعرض الواجهة ذلك بوضوح؛ لا تختلق تاريخًا.

## خطة Migration — مطلوبة قبل التفعيل، لم تُنفّذ

1. احفظ نسخة من القواعد المنشورة وإعداد الحماية وسجلات adminAccess الموجودة، وحدد الإصدار المختبر. لا تستبدل القواعد القديمة دون مراجعة توافق Production مستقلة.
2. من بيئة موثوقة ومصرح بها تملك وصول Firebase Admin الرسمي أصلًا، احصر **جميع** الحسابات ذات `admin: true`. لا تنشئ service account أو token يدويًا. لا تستخدم قائمة مستنتجة من البريد أو الهاتف.
3. بعد موافقة منفصلة، جهّز `adminSecurity/config` مع `enabled: false` وقائمة المعرفات الكاملة. السكربت الجديد ينشئ سجلات حماية المالكين الناقصة في نفس المعاملة. احتفظ بالـcustom claims الحالية كما هي. تعيين مالك جديد مستقبلًا يحتاج إجراء صيانة موثوقًا منفصلًا؛ إعادة تشغيل سكربت التهيئة لا تضيف claim holders جددًا.
4. بعد موافقة نشر منفصلة: راجع وانشر القواعد الجديدة، ثم واجهة الفرع المعتمدة. أثناء هذه المرحلة يظل التفويض معطّلًا والمدير القديم يعمل. الموقع القديم لا يدعم واجهة المساعدين.
5. تحقق من القواعد والواجهة والحساب المالك في بيئة النشر، ثم فعّل `enabled: true` من البيئة الموثوقة فقط. بعدها أضف المساعدين من الواجهة بصلاحيات الحد الأدنى. أي نقص في قائمة المالكين **مانع لتفعيل التفويض**؛ Rules لا تستطيع قراءة custom claim لحساب آخر.
6. اختبر حساب مساعد مخصصًا بعد الموافقة. الرجوع الآمن يبدأ بتعطيل التفويض من البيئة الموثوقة، ثم إعادة الإصدار/القواعد المحفوظة حسب خطة النشر. لا تحذف سجلات التدقيق.

لم يُنفّذ أي بند يكتب إلى Production، ولم تتغير Authentication أو indexes أو إعدادات Firebase.

## الاستعلامات والفهارس

- قائمة المساعدين: `adminAccess where role == admin_assistant orderBy __name__ limit 50`، مع cursor للصفحات التالية.
- الإحصاءات: count لنفس role، ثم role مع `adminStatus == active` أو `suspended`.
- البحث: `users where phoneNumber == value limit 10` أو `email == value limit 10`.
- لم تُفحص فهارس Production في هذه المهمة ولم تُنشر فهارس. قبل النشر تحقق من فهارس الحقول وعدم وجود إعفاءات تمنع الاستعلامات أعلاه؛ عند طلب Firestore فهرسًا مركبًا للإحصاءات راجع فهرس `adminAccess(role ASC, adminStatus ASC)` في بيئة المراجعة. نجاح Emulator لا يثبت وجود فهرس Production.

## الاختبارات المحلية

`npm test` يشغل اختبارات UI السابقة واختبارات نموذج الصلاحيات. `npm run test:browser` يشغل المتصفح العام وAuth السابق ودورة المساعدين؛ جميع Firebase SDK calls في اختبارات المتصفح محاكاة مع رفض أي شبكة خارج الاختبار.

`npm run test:rules` يحتاج Firestore Emulator على `127.0.0.1` عبر `FIRESTORE_EMULATOR_HOST`. تستخدم الاختبارات مشاريع `demo-*` فقط، وتفشل دون عنوان Emulator محلي. يتضمن اختبارات الموقع القديمة وV2 والتصعيد والتفويض وتدقيق المساعدين.

صور المراجعة بيانات اصطناعية فقط. لا يلزم نشر Backend لاستعمال الميزة بعد التهيئة الموثوقة؛ العمليات الحساسة محمية بالقواعد. إدارة claims وحذف Authentication النهائي والتحقق الإداري من حسابات الآخرين تبقى عمليات موثوقة خارج هذه الواجهة.

التحقق المحلي بتاريخ 2026-09-06: **397/397 PASS**، موزعة إلى UI 87، نموذج الصلاحيات 18، Auth/browser السابقة 110، المتصفح العام 15، متصفح المساعدين 26، Rules الأساسية 52، Rules V2 عدد 31، Rules المساعدين 58. بقيت اختبارات المرجع السابقة 212 كاملة. المقاسات 1280×900 و1366×768 و360×800 نجحت، دون overflow أفقي أو أخطاء JavaScript في المتصفح المعزول.

صور المراجعة والسجلات محفوظة خارج المستودع في `F:\SouqAlhalal-Backups\souq-alhalal-assistants-review-2026-09-06`. لم يُنشأ Commit أو Push أو Merge أو Deploy أثناء التطوير.
