# تقرير لوحة الإدارة v2 — مراجعة محلية

## A. Git

- قبل العمل: `main` عند `f597664`، working tree وstaging نظيفان، لا ملفات untracked.
- تم فحص status وbranch وآخر 10 commits وdiff وstaged diff قبل التعديل.
- أنشئ `admin-dashboard-v2` من الحالة الحالية. لا commits أو push أو merge أو PR أو deploy.
- بعد العمل: التغييرات أدناه محلية وغير staged للمراجعة. لم تتغير معرفات Firebase أو التطبيق.

## B. الملفات

جديد:
- `admin-dashboard.js`: تبويبات الإدارة، تفاصيل وروابط السجلات، pagination، count aggregation، الإجراءات والبلاغات.
- `admin-dashboard.css`: لوحة فاتحة RTL، بطاقات مرنة، وتوسيع modal الإدارة على الكمبيوتر.
- `tests/admin-rules.test.mjs`: اختبارات الصلاحيات والإشراف على محاكي محلي معزول.
- هذا التقرير و`tests/ADMIN_V2_RESULTS.md`: النطاق والقيود ونتائج كل اختبار.

معدل:
- `app.js`: دمج اللوحة تدريجيًا مع Firebase الحالي، أزرار الإبلاغ، إخفاء المزادات المرتبطة بإعلان خاضع للمراجعة، وتسجيل قرارات الخدمات والحذف. إبقاء لوحة الخدمات الأصلية مع pagination وإلغاء قراءات الأسماء لكل صف. مسح بيانات الإدارة عند تغير جلسة المصادقة.
- `index.html`: تحميل CSS الجديد فقط.
- `firestore.rules`: حالة الحساب، metadata الإشراف، reports، وسجل الإدارة.
- `package.json`: تشغيل اختبارات القواعد الجديدة بعد القديمة.
- `tests/auth-browser.test.cjs`: محاكاة إضافات SDK، اختبارات Dashboard والجوال، HTTPS محلي مع اعتراض كامل للشبكة لعمل Web Crypto. اختبارات لوحة الخدمات القديمة تستدعي الآن اسمها `openAdminServices`.
- `tests/ui.test.mjs`: تحديث assertion قديم كان يمنع مجرد وجود اسم `adminUid`، ليتوافق مع حقل سجل الإدارة؛ custom claim يبقى أساس التفويض.

## C. لوحة الإدارة والبيانات الفعلية

سبعة تبويبات: الرئيسية، المستخدمون، الإعلانات، المزادات، البلاغات، طلبات الخدمات، سجل الإدارة.
الإحصاءات تستخدم count aggregation مستقلاً عن صفحات العرض؛ الفشل يعرض «تعذر الإحصاء» وليس صفرًا. لا fixtures داخل التطبيق.
صفحات 50 سجلًا مرتبة بمعرف المستند، تحميل التفاصيل عند الطلب، بيانات المستخدم المرتبطة أول 20 سجلًا مع عدد كامل. لا N+1 في قوائم الإدارة. لوحة الخدمات وطلبات الحذف لهما صفحات تالية وأولى؛ فلاترهما الأصلية تعمل على الصفحة المعروضة.

فحص البنية وجد `users`, `animals`, `auctions`, `purchaseRequests`, `serviceRequests`, `conversations/messages/privateContacts`, `accountDeletionRequests`.
لم يوجد `bids` أو reports أو adminAuditLogs قبل العمل. المزاد يحتفظ بـ `currentPrice`, `lastBidderId`, `lastBidAt` فقط، فلا يمكن استخراج عدد المزايدات أو سجل تاريخي كامل. لا تغيير لمنطق سعر البداية أو الزيادة أو العداد أو قرار البيع والفائز.
الاشتراك المدفوع أزيل سابقًا؛ شاشة الحساب تعرض خطة مجانية. حقول الاشتراك الاختيارية تعرض إن كانت موجودة، ولا توجد أعداد اشتراكات وهمية.
الهاتف والبريد في تفاصيل المستخدم يعرضان فقط إن كانا محفوظين أصلًا في الملف؛ البريد غير محفوظ حاليًا في Firestore ولا نقرأ Firebase Auth directory.

## D. المستخدمون

بحث داخل الصفحة بالاسم والهاتف والبريد والمعرف، وبحث مطابق شامل بالـUID أو الاسم الكامل أو الهاتف أو البريد. فلاتر النوع والحالة والدولة تطبق على الخادم. البحث الجزئي الشامل يحتاج فهرس بحث موثوقًا؛ النص يوضح نطاق البحث.
تفاصيل الحساب وروابط إعلاناته ومزاداته وآخر مزايداته المعروفة وطلبات الشراء وبلاغاته والبلاغات ضده، مع count مستقل. حقول غائبة تظهر «غير مسجل».
تعليق، حظر، إعادة تفعيل، طلب حذف إداري، مع تأكيد وسبب وtransaction يكتب audit. لا يسمح تعديل حالة المسؤول نفسه من الواجهة أو القواعد.
طلب الحذف الإداري يضع `users.status = deletion_requested` ويوقف الكتابة؛ يظهر بفلتر المستخدمين. مسار `accountDeletionRequests` الأصلي محفوظ لطلبات المستخدمين ومعالجتها. لا ادعاء بحذف Auth أو البيانات فعليًا.

## E. الإعلانات والصور

الصور الحالية JPEG Data URLs داخل `animals.images`؛ لا استخدام فعلي لـFirebase Storage API.
لكل صورة زر حذف واضح. transaction يعيد قراءة animalId ويبحث بقيمة الصورة الكاملة، ولا يستخدم index. يحذف القيمة المحددة فقط؛ إذا تكررت القيمة نفسها يحذف نسخها المتطابقة. إعادة ترتيب الصور أو إضافة صور متزامنة لا تضيع التحديثات الأخرى.
بعد الحذف: `imagesLocked=true`, `moderationLocked=true`. القواعد تمنع البائع من تغيير images أو إزالة metadata أو إعادة تنشيط الإعلان أو حذفه ثم إعادة إنشائه بنفس ID. يبقى الانتقال من active إلى sold/not_approved متاحًا لإتمام البيع أو إغلاق المزاد، دون السماح بإعادة إظهار hidden/needs_review. يمكنه تعديل وصفه وبياناته الأخرى وفق القواعد القديمة.
آخر صورة ينقل الإعلان إلى `needs_review` ويزيله من بطاقات البيع والمزاد العامة. السجل الإداري يحتوي SHA-256 للصورة فقط، لا Base64. لا حذف لإعلان كامل ولا Storage object.
قفل الصور محافظ ومقصود: البائع لا يستطيع رفع بديل على الإعلان المقفل حاليًا. إضافة مسار رفع بدائل مع مراجعتها تحتاج تطويرًا منفصلًا. المنع لا يعالج إعادة ضغط الصورة أو إنشاء إعلان جديد بمعرف مختلف؛ هذا يحتاج كشف محتوى موثوقًا.

## F. البلاغات

`reports/{reporterUid_targetType_targetId}`: reporterId, targetType (user/animal/auction), targetId, reportedUserId, reason, details, status=open, createdAt.
تحديث الإدارة فقط يضيف reviewedBy/reviewedAt/resolutionNotes/moderationLogId، والحالات reviewing/resolved/rejected. سبب المعالجة هو ملاحظاتها المسجلة.
الهدف ومالكه يتحقق منهما في القواعد. حقول إنشاء محددة، أطوال مقيدة، timestamp موثوق، بلاغ واحد لكل مستخدم/هدف؛ إعادة الإنشاء أو تعديل البلاغ من المستخدم ممنوعان. المبلّغ عنه لا يقرأ البلاغ أو هوية المبلّغ.

## G. Audit Log

`adminAuditLogs`: adminUid, action, targetType, targetId, reason, timestamp, metadata محدودة إلى imageHash اختياري.
تسجل معاملات المستخدم والصورة وحالة الإعلان ومعالجة البلاغ، وكذلك قرارات الخدمات ومتابعة طلب الحذف الأصلي.
القراءة والإنشاء للمسؤول صاحب custom claim فقط؛ update/delete ممنوعان حتى للمسؤول عبر قواعد العميل.
تغييرات users/animals/reports الجديدة تتطلب audit مطابقًا لنفس الهدف ووقت transaction باستخدام getAfter. إجراءات الخدمات والحذف القديمة تكتب audit في معاملتها، لكن قواعدها الأصلية لم يُفرض عليها رابط audit جديد حفاظًا على توافقها؛ يمكن لعميل إداري معدل تجاوز تسجيل تلك الإجراءات القديمة.

## H. Firestore Rules

- `activeAccount()` يمنع الكتابة للحسابات ذات حالة تقييد صريحة. الملفات القديمة غير الموجودة أو التي لا تحتوي status تبقى متوافقة مع السلوك القديم.
- `isAdmin()` يتطلب claim admin=true وحسابًا غير مقيد. معرفة UID أو إظهار الزر لا يمنح الصلاحيات.
- البائع لا يغير حالة حسابه أو metadata الإشراف أو إعلان غيره أو يعيد الصور المقفلة. إغلاق إعلان نشط إلى sold/not_approved يبقى متاحًا دون إلغاء القفل.
- المزاد المرتبط بإعلان مخفي/خاضع للمراجعة لا يقبل مزايدة جديدة؛ شرط endTime/minIncrement/الهوية باقٍ.
- قراءة purchaseRequests أضيفت للمسؤول للإحصاء والتفاصيل؛ لا صلاحية إدارية لقراءة محادثات الآخرين أو privateContacts.
- reports حقول وأهداف مقيدة؛ audit append-only وmetadata محدودة.
- قراءة animals/auctions العامة القديمة بقيت كما هي للتوافق. الإخفاء يزيل الإعلان من واجهة السوق ولا يجعله مستندًا سريًا؛ الصورة المحذوفة تُزال فعليًا من المستند.
- القواعد محلية ولم تُنشر. تشغيل الميزات على Firebase الحقيقي قبل مراجعة ونشر القواعد لاحقًا لن يكون مكتملًا أو محميًا بهذه التغييرات.

## I. الاختبارات

تفاصيل اسم كل اختبار وPass في `tests/ADMIN_V2_RESULTS.md`.
- UI: 87/87.
- Firestore legacy: 52/52، Firestore moderation: 31/31.
- Auth/browser/dashboard: 43/43، ومجموعة browser الأساسية: 15/15.
- المجموع بعد المراجعة النهائية: 87 + 52 + 31 + 43 + 15 = 228. الرقم السابق 226 كان 87 + 52 + 30 + 42 + 15؛ أضيف اختباران للانحدار المكتشف أثناء المراجعة. فحوص Syntax وgit diff --check خارج هذا العدد.
- Syntax لـapp.js/admin-dashboard.js/market-ui.js/payment-demo.js وgit diff --check ناجحة.
- المحاكي: loopback 127.0.0.1:8185، مشروعا demo-souq-launch وdemo-souq-admin فقط. لا بيانات إنتاج.
- Edge headless: شبكة Firebase والموقع معترضة بالكامل، الهاتف والبريد والبيع والشراء والمزاد والخدمات محاكاة. لا SMS أو Auth حقيقي أو دفع أو كتابة خارجية.
- فحص صور Dashboard بعرض 360 و1280؛ لا overflow. اختبار الصورة يغيّر ترتيب الصور ويضيف صورة قبل transaction ويتحقق من الحفاظ عليها.

## J. Requires trusted backend / Cloud Function

حذف مستخدم Firebase Auth نهائيًا أو تعطيله وإبطال جلساته؛ تنفيذ سياسة محو/إخفاء البيانات؛ سجل تدقيق موثوق ضد مسؤول معدل العميل؛ منع spam بمعدل زمني قوي وApp Check؛ كشف إعادة رفع محتوى مخالف بصيغة أخرى؛ حذف Storage objects إذا انتقلت الصور إلى Storage مستقبلًا؛ حفظ سجل مزايدات موثوق ومقاييسه؛ بحث نصي جزئي شامل عند الحاجة.
لا Admin SDK أو service account أو tokens جديدة داخل المتصفح أو المشروع.

## K. حدود وأعمال متبقية

- لا سجل مزايدات تاريخي أو إجماليات قابلة للاستخراج من البنية الحالية؛ لا تزوير فائز أو أرقام.
- واجهة الخدمات الأصلية محفوظة وتفتح من تبويب الخدمات مع زر رجوع؛ فلاترها على الصفحة فقط وتعرض المعرفات بدل جلب أسماء كل صف.
- counts ليست snapshot واحدة ذرية عبر جميع collections؛ اختلافات لحظية طبيعية أثناء تحديث البيانات. count الاستعلامات وفلاترها قد تتطلب فهارس في مشروع حقيقي؛ لم نفحص أو نغير إعدادات production.
- صفحات التفاصيل المرتبطة تعرض 20 سجلًا مع إجمالي مستقل؛ القوائم الأساسية قابلة للتصفح. بعض counts، مثل البلاغات على إعلان، تعرض في التفاصيل لتجنب قراءات لكل صف.
- الحالات المفقودة أو المستقبلية لا تدخل بطاقات الحالات المحددة لكنها تدخل الإجمالي وتظهر في القوائم.
- حظر Firestore لا يحذف Auth ولا يمنع قراءة مستندات السوق العامة. القفل لا يمحو نسخة صورة سبق تحميلها لدى مستخدم آخر.
- التجربة على بيانات production والأجهزة الفعلية وتسليم SMS ونشر Rules لم تنفذ، بحسب قيد العمل المحلي.

## L. ملخص التغيير

إضافة وحدة Dashboard وCSS وقواعد moderation واختبارات وتقارير، مع دمج محدود داخل app.js. لا إعادة كتابة التطبيق، لا تغيير إعدادات Firebase ولا مسارات التسجيل أو منطق المزاد الأساسي. نقطة التوقف: تبقى التغييرات على admin-dashboard-v2 للمراجعة دون نشر أو دمج.

### المراجعة النهائية قبل Commit

صححت المراجعة خطأين من التعديلات: فلتر الإشراف كان يخفي نتائج sold/not_approved، وقفل الحالة كان يمنع إغلاق إعلان نشط بعد حذف صورة إدارية. أضيفت اختبارات تثبت عرض نتائج المزاد مع استمرار إخفاء hidden/needs_review، والسماح بإتمام البيع مع استمرار منع إعادة التنشيط والصورة. صُحح أيضًا عامل `<=` في mock المتصفح الذي احتوى مسافة زائدة. لم يحدث نشر أو تعديل لبيانات الإنتاج أو إنشاء مفاتيح أو Commit.
