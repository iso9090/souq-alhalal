const http=require('node:http'),fs=require('node:fs'),path=require('node:path');const root=path.resolve(__dirname,'..'),port=Number(process.env.MELKAK_PORT||8786);const allowed=new Set(['index.html','image-provider.js','livestock-images.js','commercial-model.js','admin-permissions.js']);const types={'.html':'text/html; charset=utf-8','.js':'text/javascript; charset=utf-8','.css':'text/css; charset=utf-8','.json':'application/json; charset=utf-8','.webp':'image/webp','.svg':'image/svg+xml','.ttf':'font/ttf'};http.createServer((req,res)=>{const url=new URL(req.url,'http://localhost');let rel=decodeURIComponent(url.pathname).replace(/^\//,'')||'index.html';const file=path.resolve(root,rel);if(!['GET','HEAD'].includes(req.method)||!file.startsWith(root+path.sep)||!(allowed.has(rel)||rel.startsWith('melkak/')||rel.startsWith('fonts/'))||!fs.existsSync(file)||!fs.statSync(file).isFile()){res.writeHead(404);return res.end('Local preview resource not found');}res.setHeader('Cache-Control','no-store');res.setHeader('Content-Type',types[path.extname(file)]||'application/octet-stream');res.end(req.method==='HEAD'?'':fs.readFileSync(file));}).listen(port,'127.0.0.1',()=>console.log('MELKAK local-only preview: http://localhost:'+port));
